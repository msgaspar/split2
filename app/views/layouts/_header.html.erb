<header class="py-6">
  <div class="flex flex-wrap items-center gap-6 sm:flex-nowrap">
    <h1 class="text-lg font-semibold leading-7 text-gray-900"><%= link_to 'split', root_path %></h1>
    <div class="order-last flex w-full gap-x-8 text-sm font-semibold leading-6 sm:order-none sm:w-auto sm:border-l sm:border-gray-200 sm:pl-6 sm:leading-7">
      <!-- <a href="#" class="text-indigo-600">visão geral</a> -->
      <!-- <a href="#" class="text-gray-700">All-time</a> -->
      <%= link_to 'ver todos os registros', registros_path, class: 'font-semibold text-gray-700' %>
    </div>
    <div class="ml-auto font-semibold text-gray-900">
      <% if authenticated? %>
        <%= button_to 'sair', session_path, method: :delete, class: "hover:text-indigo-500" %>
      <% end %>
    </div>
  </div>
</header>